sudo: required
language: java

jdk:
- oraclejdk8

services:
- docker

env:
  global:
    - COMMIT=${TRAVIS_COMMIT::7}
    - REPOSITORY=slawekm/openregister-loader

cache:
  directories:
  - $HOME/.gradle/caches
  - $HOME/.gradle/wrapper

script:
- ./gradlew clean build
- echo $COMMIT
- docker build -t $REPOSITORY:$COMMIT .
- docker tag $REPOSITORY:$COMMIT $REPOSITORY:travis-$TRAVIS_BUILD_NUMBER
- docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
- docker push $REPOSITORY
